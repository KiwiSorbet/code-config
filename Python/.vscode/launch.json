{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "args": "${command:pickArgs}",
            "stopOnEntry": false,
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
